MEM.learned.notion = function( p ){
	this.init();
};

MEM.learned.notion.prototype.init = function(){
	this.chronology = [];
};

MEM.learned.notion.prototype.add_action = function( action ){
};

MEM.learned.notion.prototype.merge = function( matching_notions ){
};